# raw/codex-jsonl

Store raw `codex exec --json` captures here.

Minimum coverage:

- happy path run;
- approval-required or failed run;
- tool and command activity;
- delegated or forked thread references when available.

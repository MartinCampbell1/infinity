# References

This tree contains local read-only snapshots copied from external reference repositories.

Rules:

- never edit files in the original source repositories;
- if implementation work is needed, copy into `apps/*` first;
- `references/*` exists for auditability, comparison, and source tracing.

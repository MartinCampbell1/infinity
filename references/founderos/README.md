# FounderOS Reference

Source repo:

- `/Users/martin/FounderOS`

Purpose:

- shell/navigation reference
- execution route family reference
- route-scope and shell diagnostics reference

Policy:

- treat this directory as a copied local snapshot
- do not use it as the editable implementation root
- make changes only under `/Users/martin/infinity/apps/shell`

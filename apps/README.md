# Apps

This tree contains the editable product implementation for Infinity.

Structure:

- `apps/shell` = FounderOS-derived operator shell implementation
- `apps/work-ui` = Open WebUI-derived workspace implementation

Rules:

- edit only inside `apps/*`
- never edit the upstream source repos directly
- copy from `references/*` or external repos first, then adapt locally
